@model IEnumerable<PetHealthAssistantProject.Models.Vet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VetHospitalName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VetSpeciality)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VetAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VetAppointment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VetHospitalName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VetSpeciality)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VetAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VetAppointment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>

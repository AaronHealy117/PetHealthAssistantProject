@model IEnumerable<PetHealthAssistantProject.Models.Walks>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WalksPark)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WalksAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WalksPark)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WalksAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WalksID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WalksID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WalksID })
        </td>
    </tr>
}

</table>
